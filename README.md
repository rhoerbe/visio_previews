# export PNG images from all Visio files in a given directory

Set input and output path and exclusions in config.yaml


## Issues
- Previews are canvas sized, not the selection of all shapes -> may be lots of whitespace